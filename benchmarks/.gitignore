# Local run outputs, if RAZOR_BENCH_RUNS points them back into this folder.
# By default runs land in the system temp dir, outside the repo entirely.
runs/

# Run records stay on the operator's machine; the repo carries the harness.
records/

# Any other run output stays local too — the repo carries the harness only.
results/
records-archive/
